{% extends layout %}

{% block main %}

    <div class="page-title">
        <h1 class="title">{{ pagina.getTitle()|raw }}</h1>
    </div>

    <div class="p-3">
        {{ pagina.getTexto()|raw }}
    </div>

{% endblock %}
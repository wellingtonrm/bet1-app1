{% extends layout %}

{% block main %}

    <div class="page-title">
        <h1 class="title">{{ pagina.getTitle()|raw }}</h1>
    </div>

    <div class="p-3">
        <div class="block-text">
            {{ pagina.getTexto()|raw }}
        </div>
    </div>

{% endblock %}